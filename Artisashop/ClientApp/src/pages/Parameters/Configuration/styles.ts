import styled from "styled-components";

export const ConfirmButtonWrapper = styled.div`
  position: relative;
  left: 62px;

  & > div {
    width: 280px;
    height: 54px;
  }

  & > div > button {
    height: 100%;
  }
`;
